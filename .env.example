# API Configuration
# This URL is used for direct API calls that don't go through the Vite proxy
# For authentication and other API calls, we use the Vite proxy configured in vite.config.ts
# Note: All API endpoints start with /api/ and include versioning (e.g., /v1/)
VITE_API_URL=http://localhost:8080
